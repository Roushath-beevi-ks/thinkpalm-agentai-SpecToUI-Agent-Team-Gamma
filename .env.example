# LLM: set ONE of the keys below. Optional: force provider with AI_PROVIDER=anthropic | openai
# Default order if AI_PROVIDER is unset: Anthropic (if ANTHROPIC_API_KEY is set), else OpenAI.

# AI_PROVIDER=anthropic

ANTHROPIC_API_KEY=
# Optional override (default: claude-3-5-sonnet-20241022)
# CLAUDE_MODEL=claude-3-5-sonnet-20241022

OPENAI_API_KEY=
# Optional override (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
